package org.openfinna.java.connector.classes.models.libraries;

import java.io.Serializable;

public enum LibraryType implements Serializable {
    MUNICIPAL(0),
    MOBILE(1);

    LibraryType(int type) {
    }
}
